name: fission-auth
version: 0.1.0

authors:
  - Ethan Knapp

crystal: 1.10.0

license: MIT

dependencies:
  kubernetes:
    github: Matchlighter/crystal-kubernetes
    branch: support-token-auth

targets:
  fission-auth:
    main: main.cr
